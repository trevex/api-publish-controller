apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: shirts.stable.example.com-role
rules:
  - verbs:
      - list
      - get
      - watch
    apiGroups:
      - stable.example.com
    resources:
      - shirts